<h1>Editing Book</h1>

<%= form_with model:@book, local:true do |f| %>

 <h4>title</h4>
 <%= f.text_field :title %>

 <h4>opinion</h4>
 <%= f.text_area :opinion %>

 <%= f.submit 'Update Book' %>

<% end %>

<%= link_to "show", book_path(@book.id) %>